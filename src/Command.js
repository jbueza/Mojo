function Command() {
  this.execute(params);
};

Command.prototype.execute = function(paramsObj) {
  
};

Command.prototype.onResponse = function(response) {
  
};

Command.prototype.onError = function(errors) {
  
};